{{- if .Values.networkPolicy.enabled }}
# F-12.6: the only outbound connections are to configured sinks and telemetry
# destinations — no update checks, no analytics.
#
# The binary makes no other calls, but a security reviewer should not have to
# take that on trust. This policy enforces it at the cluster level, so a future
# dependency that phones home is blocked rather than merely discouraged.
#
# Note that an empty egress rule list denies all egress. Set
# networkPolicy.allowEgressCIDRs to your object store, or the collector will be
# unable to deliver and will buffer until it hits its bound.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "trajectory.fullname" . }}
  labels: {{- include "trajectory.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels: {{- include "trajectory.selectorLabels" . | nindent 6 }}
  policyTypes: [Ingress, Egress]

  ingress:
    - from:
        {{- if .Values.networkPolicy.allowIngressFromNamespaces }}
        {{- range .Values.networkPolicy.allowIngressFromNamespaces }}
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ . }}
        {{- end }}
        {{- else }}
        # No namespaces listed: accept from within this namespace only.
        - podSelector: {}
        {{- end }}
      ports:
        - {protocol: TCP, port: 4317}
        - {protocol: TCP, port: 4318}
        - {protocol: TCP, port: 4319}

    # Metrics scraping is separate: a monitoring namespace should be able to
    # reach /metrics without being able to push telemetry in.
    - ports:
        - {protocol: TCP, port: 9464}

  egress:
    {{- if .Values.networkPolicy.allowDNS }}
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - {protocol: UDP, port: 53}
        - {protocol: TCP, port: 53}
    {{- end }}

    {{- range .Values.networkPolicy.allowEgressCIDRs }}
    - to:
        - ipBlock:
            cidr: {{ . }}
      ports:
        - {protocol: TCP, port: 443}
    {{- end }}
{{- end }}
