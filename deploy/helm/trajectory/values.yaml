# Copyright The Trajectory Authors.
# SPDX-License-Identifier: Apache-2.0

image:
  repository: ghcr.io/trajectory-project/trajectory
  tag: ""          # defaults to .Chart.AppVersion
  pullPolicy: IfNotPresent

# One replica by default, and that is a deliberate default rather than a
# placeholder. The collector is horizontally scalable, but each replica needs
# its own buffer volume: two processes sharing one buffer directory corrupt it,
# and the collector refuses to start rather than let that happen. Scaling up
# means a StatefulSet with per-replica volumes, which is what this chart does.
replicaCount: 1

# The buffer is the only path that needs to be writable, and it is what makes
# acknowledged data survive a sink outage or a restart. Sizing it is sizing how
# long an outage you can absorb: roughly (your bytes/sec) × (tolerable outage).
buffer:
  size: 20Gi
  storageClass: ""

resources:
  requests:
    cpu: 500m
    memory: 256Mi
  limits:
    # No CPU limit by default: throttling a telemetry agent makes it drop
    # behind and buffer, which turns a CPU spike into a disk problem.
    memory: 1Gi

# Security context. F-12.5 requires non-root, read-only root filesystem, and no
# privilege escalation. These are not tunables that a deployment should relax;
# they are here so a reviewer can see them.
securityContext:
  runAsNonRoot: true
  runAsUser: 65532
  runAsGroup: 65532
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop: ["ALL"]
  seccompProfile:
    type: RuntimeDefault

service:
  type: ClusterIP
  otlpGRPC: 4317
  otlpHTTP: 4318
  native: 4319
  metrics: 9464

# Secrets are referenced, never inlined (F-11.1). Create the secret separately:
#
#   kubectl create secret generic trajectory-keys \
#     --from-literal=hmac-key=$(openssl rand -hex 32)
#
# Rotating the HMAC key breaks joinability of new records against old ones, so
# treat it as long-lived and note the date you set it.
secrets:
  name: trajectory-keys
  hmacKeyRef: hmac-key

# F-12.6: the only outbound connections are to configured sinks and telemetry
# destinations. The NetworkPolicy below enforces that at the cluster level
# rather than trusting the binary, which is what a security reviewer will ask
# for.
networkPolicy:
  enabled: true
  # Namespaces allowed to send telemetry in.
  allowIngressFromNamespaces: []
  # CIDRs the collector may reach, which should be your object store and your
  # telemetry backend and nothing else.
  allowEgressCIDRs: []
  # DNS is required for resolving the sink endpoint.
  allowDNS: true

serviceMonitor:
  enabled: false
  interval: 30s

# The collector configuration. This is mounted as a file and validated by
# `cc validate` in an init container, so a bad config fails the rollout rather
# than crash-looping.
config:
  schema_version: "0.1.0"
  tenant: CHANGEME

  sources:
    - name: otlp
      type: otlp
      http: {listen: "0.0.0.0:4318"}
      grpc: {listen: "0.0.0.0:4317"}

  assembly:
    session_key: ["session.id", "gen_ai.conversation.id", "trace_id"]
    window: 5m
    settle_after_terminal: 5s
    max_in_flight: 50000

  redaction:
    default: deny
    on_error: quarantine
    allow:
      - "steps[*].content_inline"
      - "steps[*].raw"
    rules:
      - id: email
        match: {regex: "[\\w.+-]+@[\\w-]+\\.[\\w.]+"}
        action: tokenize
    tokenization:
      key_env: CC_HMAC_KEY

  entities: []

  sampling:
    head: {rate: 1.0}
    tail:
      keep_if:
        - "status != 'complete'"
        - "has_error"
      otherwise_rate: 1.0

  buffer:
    dir: /var/lib/cc/buffer
    max_bytes: 16Gi
    max_age: 24h
    backpressure_at: 0.8

  sinks:
    - name: lake
      type: s3
      bucket: CHANGEME
      prefix: trajectory/
      region: us-east-1
      partition_by: ["dt", "tenant", "task_type"]
      compression: zstd

  telemetry:
    metrics: {listen: "0.0.0.0:9464"}
    log_level: info
